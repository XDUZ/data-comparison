import request from '@/utils/request';

/**
 * 获取数据对比列表
 * @param {*} params
 * @returns
 */
export const getDataComparisonList = (params) => request({
  url: '',
  method: 'post',
  data: params,
});

/**
 * 新增数据对比
 * @param {*} params
 * @returns
 */
export const addDataComparison = (params) => request({
  url: '',
  method: 'post',
  data: params,
});
